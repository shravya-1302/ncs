{
  "name": "server",
  "version": "1.0.0",
  "description": "NCS backend server",
  "main": "src/server.js",
  "scripts": {
    "dev": "nodemon src/server.js",
    "start": "node src/server.js",
    "db:schema": "mysql -u root -P 3307 ncs < database/schema.sql",
    "db:seed": "mysql -u root -P 3307 ncs < database/seed.sql",
    "db:setup": "npm run db:schema && npm run db:seed",
    "db:admin": "node scripts/createAdmin.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bcryptjs": "^3.0.2",
    "cors": "^2.8.5",
    "dotenv": "^17.2.3",
    "express": "^5.2.1",
    "jsonwebtoken": "^9.0.3",
    "multer": "^2.0.2",
    "mysql2": "^3.15.3"
  },
  "devDependencies": {
    "nodemon": "^3.1.14"
  }
}